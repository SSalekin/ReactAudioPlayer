Audio player component using react.js

```
space = play/pause
→ = forward by 1sec
← = backward by 1sec
```

```
git clone git@github.com:SSalekin/ReactAudioPlayer.git
cd ReactAudioPlayer
npm install
npm start
```
